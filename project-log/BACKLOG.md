# BACKLOG

Open items, deferred work, known limitations. Tagged by stage and severity.

| ID | Stage | Severity | Item |
|---|---|---|---|
| B-01 | Modelling | low | Quantile regression models for prediction intervals. Rejected for v1 in favour of held-out residual quantiles (D-11). Revisit if the residual-quantile interval proves poorly calibrated across price bands |
| B-02 | Reporting | low | Power BI market dashboard — price per marla by locality, supply mix, size distributions. Rejected for v1 (D-06). Possible phase 2 and a separate portfolio piece |
| B-03 | Modelling | medium | Recency holdout instead of random split, if listings carry a usable posting date. Decide at modelling stage |
| B-04 | Acquisition | ~~medium~~ | **CLOSED 6 Sep.** Coordinates present on 99.6% of in-scope rows and validated inside the Lahore bounding box. No geocoding lookup table needed. Now also the basis of the B-07 fingerprint proposal |
| B-05 | EDA | high | Acceptance criteria are provisional until locked at EDA close (D-09). Must not reach validation unlocked |
| B-06 | Cleaning | ~~medium~~ | **CLOSED 8 Sep, D-45 — retired, not tuned: there are no false merges for a threshold to separate.** 15% price-spread threshold for collapsing near-duplicate groups is provisional. Validate against real spread distribution at cleaning stage |
| B-07 | Cleaning | ~~high~~ | **CLOSED 8 Sep, D-43 — 77.1% of rows in multi-groups down to 33.0%.** **D-08's fingerprint is too coarse to apply as written.** Locality + area + beds + baths puts 6,420 of 8,459 in-scope listings (75.9%) into multi-listing groups, and only 46% of those fall inside the 15% spread. The cause is `locality` granularity, not the threshold: 1,277 listings carry locality "DHA" with no phase, so one fingerprint covers dozens of distinct houses. Applying D-08 unchanged would merge genuinely different properties. Proposal: rebuild the fingerprint on coordinates (present on 99.6% of rows, B-04 closed) plus beds/baths/area. Decide at cleaning, amend D-08, and re-evaluate B-06 afterwards — the threshold cannot be judged until the fingerprint is right |
| B-08 | Feature eng. | ~~high~~ | **CLOSED 8 Sep, D-40.** **`locality` is too coarse for the dominant societies.** "DHA" is 1,277 rows (15%) with no phase. Quantified 6 Sep from `address`: Phase 6 median 5,650,000/marla vs Phase 1 3,500,000/marla — a 61% spread inside one categorical value. `Phase` appears in 1,265 of 1,277 addresses, `Block` in 670, plus two named sub-societies: **EME** (96 rows) and **Rahbar** (17). Decomposition of the large societies is required, not optional. Caution: the 17 Rahbar rows price near DHA main phases, which is not typical for Rahbar — inspect rather than trust them |
| B-09 | Cleaning | ~~high~~ | **CLOSED 8 Sep, D-44 — 39 rows.** **Junk price and area values.** Two distinct causes, now separated. (a) **Unit errors — see B-15**, which account for the extreme *high* price-per-marla outliers: "2 Kanal" recorded as 2 marla makes a 420M listing read as 210M/marla instead of a plausible 10.5M. (b) **Genuine price junk**, e.g. PKR 53,000 for a 10-marla house and PKR 20,000 for a 503 sq ft listing — figures that read as monthly rent. Fix (a) first, then re-derive the outlier list; the residual after unit correction is the real junk |
| B-10 | Cleaning | ~~low~~ | **CLOSED 8 Sep, D-41 — five merges, not three.** Three locality spelling variants to reconcile: Al Kabir/Al-Kabir Orchard, Sher Shah/Shershah Colony, High Court/Highcourt Society. The vocabulary is otherwise clean — 3 collisions in 387 values |
| B-11 | Acquisition | low | 10 listings (0.11%) persistently return Ilaan's "Property Not Found" template after retries. Whether delisted or persistently failing is not established; the same template was served transiently for 969 live listings. Recorded as an unexplained gap, not as removals |
| B-12 | Cleaning | ~~high~~ | **CLOSED 8 Sep, D-31.** **Canonical area unit: marla** (78% of rows already marla; Lahore convention). Conversions verified self-consistent 6 Sep: normalised price-per-marla medians agree across units (marla 3.60M, kanal 4.00M). Add `area_marla` as a NEW column; leave `area` and `area_unit` untouched, so the conversion stays auditable and a wrong factor remains detectable |
| B-13 | Cleaning | ~~medium~~ | **CLOSED 8 Sep, D-37 — 14 columns dropped, not 7; is_featured and has_video kept.** **Zero-variance columns, safe to drop:** `condition` (all "New"), `year_built` (all 2026 — defaulted to the current year at render, not a build year), `is_verified`, `is_gold_verified`, `is_sold` (all False), `is_featured` (13 True of 8,459), `has_video` (123 of 8,459). **Explicitly NOT dropped:** `views` (0–494, median 8) and `image_count` (0–128, median 1, p90 24) vary substantially and go to EDA. Principle: drop on zero variance, which is mechanical; defer every other column decision to stage 4 after EDA. Keeping a useless column costs nothing; dropping a useful one costs a discovery never made |
| B-14 | Cleaning | low | `amenities` is empty on every row. Verified 6 Sep against cached pages: the field is `[]` in the payload even for a listing with 128 images, and no Amenities/Features section is rendered on the page. A schema field Ilaan never populates — not a parse gap, nothing recoverable, no re-scrape warranted |
| B-15 | Cleaning | ~~high~~ | **CLOSED 8 Sep, D-32 — 59 title-wins, and the rule reversed on quantity.** **`area_unit` disagrees with the title on 18 rows (0.21%) and the title is right.** Verified 6 Sep: id 677793 "2 Kanal House" carries `area=2, area_unit=marla`; id 664607 "3 Kanal House in Model Town" carries `area=3, area_unit=marla`. The payload unit is wrong at source, our extraction is faithful. The listing title states the unit and is reliable. Rule: parse the unit from `title` and cross-check against `area_unit`; where they disagree, prefer the title and log the correction. This also removes the worst price-per-marla outliers — they are unit errors, not price errors. Found by the manual spot-check (row 16), not by any automated check |
| B-16 | Cleaning | ~~medium~~ | **CLOSED 8 Sep, D-34 — plus 23 floor-sale rows B-16 did not anticipate.** **Scope leakage the type filter cannot catch: 10 penthouses** carry `type="House"` at source (e.g. "2020 Sq.ft PentHouse for Sale in Kuwait Mall, Bahria Town"). D-02 excludes flats and apartments; a penthouse is an apartment. Add a title-based secondary filter. Checked and NOT present in titles: flat, apartment, portion, plot, shop, office, farmhouse — so penthouse is the only leak |
| B-17 | Cleaning | closed | **The "Property Overview" panel is boilerplate, not data.** It renders "Property Type House / Furnished Furnished / Floor Ground / Parking Available / Property Age 0 Years / Purpose For Sale". Sampled 25 pages across the window: **1 distinct string**, identical on every page. Furnishing, floor, parking and property age are not per-listing values and extracting them would add four more zero-variance columns. No parse change, no re-scrape. Raised by the manual review; resolved 6 Sep |
| B-18 | EDA | low | **DHA Rahbar is labelled two ways and its phase number is ambiguous.** `Rahbar - Phase 2` (9 rows) and `Phase 11 - Rahbar` (5) are the same sub-society. DHA Rahbar is officially DHA Phase 11 with its own internal phases, so `loc_phase_num` reads 2 for rows that are Phase 11 at DHA level. 14 rows; flagged rather than resolved (LESSONS L-24). Decide at EDA whether Rahbar is a DHA phase or a scheme of its own |
| B-19 | EDA | medium | **B-08's Rahbar suspicion is confirmed and unexplained.** With phases decomposed (D-40), DHA Rahbar medians 4.50–4.60M/marla against DHA Phase 1 at 3.50M and Phase 4 at 3.63M. Rahbar is an outlying scheme and should be well below the main phases, not above two of them. 14 rows, so it cannot move a median-based metric; recorded because the direction is wrong, not because the volume matters |
| B-20 | Cleaning | medium | **10 rows where title and payload state areas a marla or more apart** (`area_quantity_conflict`, D-32). The payload keeps precedence as the measured field. No third source exists — the cached page renders the title string and the payload number and nothing further. Review with the size distribution in view at EDA |
| B-21 | Validation | high | **The held-out set is made of group medians (D-45), so MdAPE will not include within-group price variation.** On the 2,755 pre-collapse rows that had replication, deviation from their own group median ran 3.23% median, 7.26% p75, 13.9% p90. Validation must disclose this floor alongside the headline number, using `fp_price_spread` and `n_listings`, and the README limitation must say the model predicts a group's median asking price rather than an individual poster's ladder position |
| B-22 | EDA | medium | **Park View City is ~800 rows pre-collapse and remains the largest single society after it.** One development over-represented in training. Not a cleaning deletion — a sample-weighting question for stage 5 |
